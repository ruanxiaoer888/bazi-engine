# ENGINE-CHANGES.md — 引擎变更权威记录

> **用途**：bazi-engine 引擎（`engine/engine.dist.js` 及其源码 `tools/build_ui.py` ENGINE 标记区）每次变更的唯一权威记录。
> **给谁读**：① 本仓库新会话（对齐版本）② **bazi-app 对话**（C 端依赖本引擎，读此文件核对版本/MD5、判断是否需要回归）。
> **约定**：
> - 每次引擎变更 → 在下方**顶部**追加一条记录，并 **bump 版本号**（SKILL.md frontmatter `version` + dist 头注释）。
> - `影响C端` 列：`是` = bazi-app 需回归对应功能；`否` = 纯 UI/输入侧，dist 未变或行为不变。
> - 交付动作：`cp engine/engine.dist.js ../bazi-app/web/engine.dist.js`（只拷贝，commit 由 bazi-app 对话自行处理）。
> - 新增记录含当次 dist 的 MD5，bazi-app 侧 `md5sum web/engine.dist.js` 与下表核对，不一致即引擎有更新。

---

## v1.3.6（2026-08-19 · UI 审计批：移动端表格 + 无障碍 + 视觉统一）

> **UI 层变更（dist 不变，MD5 同 v1.3.4/1.3.5 `1A4722FA7B0974EB4F5CFA53C71AA9C3`），影响 C 端：否。**

| 项 | 值 |
|---|---|
| dist MD5 | `1A4722FA7B0974EB4F5CFA53C71AA9C3`（不变） |
| 影响 C 端 | **否**（纯 UI 层） |

**变更内容**（UI 审计 🔴-2/🟡-7,8,9,12,13/🟢-6,7,8）：
1. **移动端流月/流日 6 列表格横向滚动**：表格包 `.table-scroll{overflow-x:auto}` + ≤768px `min-width:540px`（原窄屏列挤压不可读）。
2. **无障碍**：`:focus-visible` 焦点环（键盘 Tab 可见）；chart-tabs span 注入 `role=tab/tabindex=0` + Enter/Space 键盘委托；触控目标 ≥40px（cal-toggle/report-tab/chart-tabs/chip）。
3. **视觉统一**：JS 硬编码色 6 处 → `var(--gold/--green/--red)`（特殊格局/调候/病药/提示/合婚 DST 提示）；`:root` 删未用 token（--accent/--line2）。
4. **打印样式**：`@media print` 反色（白底深字，深墨底金字打印不可读问题修复）。
5. **head 增强**：`theme-color`（移动端地址栏）+ 内联 SVG 太极 favicon（零依赖）。
6. **年份动态化**：流年/流月/流日输入去掉硬编码 2026，init 动态填当前年；placeholder 对比度提亮（--muted2→--muted）。

**bazi-app 注意事项**：dist 未变，C 端无需任何操作。

---

## v1.3.5（2026-08-19 · 引擎审计算法批 B：流月精确节气 + 合婚评分扩维）

> **UI 层变更（dist 不变，MD5 同 v1.3.4 `1A4722FA7B0974EB4F5CFA53C71AA9C3`），影响 C 端：否。**

| 项 | 值 |
|---|---|
| dist MD5 | `1A4722FA7B0974EB4F5CFA53C71AA9C3`（不变） |
| dist 大小 | 438KB（不变） |
| 影响 C 端 | **否**（改动在 UI 层 runLiuYue/analyzeHe，dist 未变） |

**变更内容**：
1. **M1 流月月柱改用 12 节精确时刻**：`runLiuYue` 原用固定参考日（midDays/refMs，正月 2/4、腊月 1/6 12:00 等）近似判月柱——临界月系统性错一月（如 1902 小寒 01-06 13:51 > 参考 12:00）。现每月取 `JIEQI.data` 中对应节的精确时刻（`JIEQI.data[year]` 即 12 节时刻数组：立春/惊蛰/…/大雪/小寒）经 `monthGZ` 判定；十二月小寒取次年 1 月。验证：2024 年正月丙寅/二月丁卯/三月戊辰与甲辰年五虎遁一致。
2. **M2 合婚「日柱关系」维度扩维 17→25 可触达**：原最高 17（五合 10+六合 7），总分天花板 92，「天生佳偶≥85」几乎不可达。新增 日柱纳音相生 +5、日支三合局 +3 → 上限 25。4000 对实测：该维最高 22、总分最高 90、≥85 佳偶 8 对（0.2%，稀有合理）。评级语义不变。

**bazi-app 注意事项**：dist 未变，C 端无需任何操作。

---

## v1.3.4（2026-08-19 · 引擎审计算法批 A：专旺阈值/规则语义/流派标注）

| 项 | 值 |
|---|---|
| dist MD5 | `1A4722FA7B0974EB4F5CFA53C71AA9C3`（LF 口径） |
| dist 大小 | 438KB（RULES 1000 条不变） |
| 影响 C 端 | **否**（专旺判定在 paipan 内，C 端不消费 special 专旺细分；规则修正仅 B 端 matchRules） |

**变更内容**：
1. **M3 专旺格阈值收紧**：0.55 → **主导行 ≥0.75 总量 + 最强他行 ≤0.15 双条件**。5000 盘对比：旧 0.94% 判专旺（远高于传统「万中无一」，含大量普通身强误标）→ 新 0.02%（1/5000，真一行独旺盘）。连锁修正格局细分/喜用/专旺断语误命中。
2. **M4 `kin_六亲_印旺母强` 语义修复**：写死「五行:火」只对土日主自洽（印星五行随日主）→ 改为「正印 + 状态:多而有力」（evalState 按日主动态判定印星五行），实测命中 9%。
3. **M5 晚子时流派标注**：UI 提示补「日柱不换、时柱按次日日干遁」流派说明（含真太阳时跨日交互说明）；流派决策留档（未改算法）。

**bazi-app 注意事项**：C 端不受影响（不消费专旺细分/规则匹配）；dist 已拷贝交付（可选替换）。

---

## v1.3.3（2026-08-19 · 引擎审计质量批）

> 引擎审计 🟢/M 项的低风险质量修复（死代码清理 + 公共 API 防御 + 文案修正）；算法决策项（流月精确节气 M1 / 合婚上限 M2 / 专旺阈值 M3 / 规则库五行写死 M4 / 晚子时流派 M5）待评估后另版本处理。

| 项 | 值 |
|---|---|
| dist MD5 | `774B7FBBE799FA5B6814DD2FD558CE8A`（LF 口径） |
| dist 大小 | 438KB（RULES 1000 条不变） |
| 影响 C 端 | **是（轻）**：`lunarToSolar` 增加输入防御（越界/非法闰月/超天数返回 null）——C 端输入受 UI 限制不会触发，但建议 C 端确认无异常路径直接消费 null |

**变更内容**：
1. **M6 `lunarToSolar` 公共 API 输入防御**：年份 1900-2099 / 月份 1-12 / 天数 1-30 / `isLeap` 与 `leapMonth` 一致性 / 天数 ≤ 当月 → 非法返回 null（UI 层已防护，导出库补防线）。
2. **M7 流年喜用文案三分支**：喜用→生用神、忌用→克用神、**非喜非忌（平运）不再输出「克用神」**（原 else 分支非喜用≠克用神，中和盘也不再误报）。
3. **🟢-1 evalState 死代码**：`xiYong.length>=5` 永不成立（xiYong 最多 3 个）→ 改 `length===0`（中和盘喜忌空，正确语义「不判假/不判冲克」）。
4. **🟢-2 `special.tiaohou` 冗余赋值删除**（L1101 被 TIAOHOU_MAP 覆盖）。
5. **🟢-8 `GAN_ORDER` 全局化**（原 matchLiuDay/matchLiuYue 各定义一份 → 全局常量）。

**bazi-app 注意事项**：C 端消费 `lunarToSolar`（农历输入），本次为其增加防御——C 端 UI 已限制输入范围，正常路径不受影响；建议 C 端对 `lunarToSolar` 返回值做一次 null 兜底（防御性，无需回归）。

---

## v1.3.2（2026-08-19 · 三方审计修复批次）

> 引擎逻辑 / UI 功能视觉 / 构建工具链 三路并行审计（子代理）后的修复。

| 项 | 值 |
|---|---|
| dist MD5 | `F73822DF49AE813B5B079E2C0BA99CFD`（LF 口径，2026-08-19 起产物强制 LF——跨平台 MD5 可比；此前的 CRLF 口径值 `AFB7FE81...` 已作废） |
| dist 大小 | 437KB（RULES 1000 条不变） |
| 影响 C 端 | **否**（改 matchLiuDay 流日规则与构建路径；C 端只消费 paipan/applyDst/SHI_CHEN_MAP，不受影响） |

**变更内容**：
1. **🔴 流日「天克地冲」判定方向错误**（引擎审计 R1）：`liuri_tkdc` 原用五行克关系（`GAN_WX[g]===WX_SK[...].克`）判定天克，与全引擎其余三处（dayun_04/liuyue_10/liunian_天克地冲日）的 `WU_CHONG` 天干相冲标准不一致——甲木日主遇庚午（真甲庚冲+子午冲）**漏判**、庚金日主遇乙子（乙庚五合，实为天合地冲）**误判**。修复为 `WU_CHONG[g]===riGan || WU_CHONG[riGan]===g`，双向验证通过（含反向场景）。
2. **跨平台构建 bug**（工具链审计 S3）：`open(ROOT + r"\ui\index.html")` 反斜杠拼接在 Linux/Mac 上写错位置（含反斜杠怪文件名），git 旧产物使 CI 全程测旧代码（假绿）；改 `os.path.join(ROOT,"ui","index.html")`。
3. **CI 门禁恒绿修复**（工具链审计 S2）：`audit_hit_distribution.js` 发现 100%/>80% 命中规则时 `process.exit(1)`（此前恒 exit 0）；`check_conflicts.js` 矛盾>0 时 `process.exit(1)`。
4. **农历年份越界 P0**（UI 审计 🔴-1）：年份下拉 1895 起但 `LUNAR_INFO` 仅 1900-2099 → 选 1895-1899/2100 农历静默算错命盘。UI 层修复：`selYear` 加范围参数、切农历重建年份(1900-2099)、`generate`/`readHe` 农历分支校验+提示。

**bazi-app 注意事项**：C 端不受影响（matchLiuDay 未被消费）；dist 已拷贝交付（MD5 `AFB7FE8191D3C475600FAB6B4F4E9A2D`，可选替换）。

---

## v1.3.1（2026-08-19 · 审计修复）

> 全项目扫描审计发现的死规则修复。v1.3.0 定版后 patch 版。

| 项 | 值 |
|---|---|
| dist MD5 | `3E4E97B260C313B4D295F5696FD11EEC` |
| dist 大小 | 437KB（RULES 1000 条不变：删 wealth_kugu 补 wealth_财官印三宝） |
| 影响 C 端 | **否**（仅 matchRules 空亡匹配修正 + 规则 1 换 1；paipan/applyDst 等 C 端消费 API 不变，C 端无需回归） |

**变更内容**：
1. **修活 `shensha_空亡` 死规则**：原「日支逢空亡」特判永不命中（60 甲子中旬空支总在下一旬，日支不可能坐本柱旬空——曾把 100% 命中改成 0 命中，两版皆死）。改为「命局任一柱地支逢日柱旬空」→ 实测 44%（200 盘），断语恢复有效。
2. **`wealth_kugu` 四神同透 0 命中**（正财偏财正印偏印全透几乎不可能）→ 换补 `wealth_财官印三宝`（正财正官正印三透，实测 1%）。
3. **audit_hit_distribution.js 新增 0 命中检测**（信息级，排除流年/流月/大运/合婚专用类；低命中规则小样本 0 命中属正常，大样本 2000 盘仍 0 才需深挖）——防死规则再次潜伏。
4. build_ui.py size 打印改字节口径（字符数→UTF-8 字节数）。

**bazi-app 注意事项**：C 端不消费 matchRules/RULES，本次无影响；dist 已拷贝交付（MD5 更新，可选替换）。

---

## v1.3.0（2026-08-19 · 定版）

> **定版声明**：断语库 1000 条里程碑稳定版。v1.2.3 的 dist 原样定版为 v1.3.0（dist 无变化，MD5 不变），git tag `v1.3.0`，发布包 `bazi-engine-v1.3.0.zip`。此后 v1.3.0 冻结，新扩充进入下一版本。

| 项 | 值 |
|---|---|
| dist MD5 | `CE99E451F5733376A56BDCE9F49D8ED4`（与 v1.2.3 相同） |
| dist 大小 | 438KB（RULES 1000 条；2026-08-19 起按文件实际字节计，此前记录为字符数口径） |
| 影响 C 端 | 是（bazi-app 按 `docs/BAZI-APP-REGRESSION-v1.3.0.md` 回归后锁定 v1.3.0） |

---

## v1.2.3（2026-08-19 · commit `338f981` 收尾点）

> 断语库 789→1000 大扩充（5 批次：`026bf4d`/`1c3aea3`/`7d03ba1`/`d89f3f9`/`338f981`），路线图 1000 目标达成。

| 项 | 值 |
|---|---|
| dist MD5 | `CE99E451F5733376A56BDCE9F49D8ED4` |
| dist 大小 | 245KB → **287KB**（RULES 内联规则 789 → 1000） |
| 影响 C 端 | **是**（规则 +211 条 → C 端单盘断语条数上升，报告篇幅变长，需回归报告生成与排版） |

**变更内容**：
1. **批次 1 神煞 +24**：神煞×十神组合（吉 18 + 凶 6），取材 shensha.md 组合解读法。
2. **批次 2 婚姻+20/健康+20**：日支十神补齐、妻夫星贵人驿马、桃花组合；健康 20 条合规仅提示+就医建议。
3. **批次 3 学业+15/六亲+15/十神组合+10**：宫位细化、日主补齐、亲缘吉神、双十神组合。
4. **批次 4 性格+20/事业+15/格局+15**：月干位置、阴干调候、神煞性格、格+透干成格/忌神组合。
5. **批次 5 用神+15/五行+12/十神组合+10/神煞+10/学业+5/六亲+5**：10 干×调候×强弱细分等；剔除 1 条 0 命中三神组合换补龙德_正财。
6. **质量红线**：每批 audit 无 100%/>80%、抽查无 0 命中；check_conflicts 1000 盘 0 矛盾；13 套回归全绿；C3 主面板 52.6→66.1（30-70 内）。

**bazi-app 注意事项**：C 端需 `cp engine/engine.dist.js web/`（已拷贝交付）后回归报告生成；MD5 核对 `CE99E451F5733376A56BDCE9F49D8ED4`；单盘断语条数显著上升，若有一屏断语数上限需调整。

---

## v1.2.2（2026-08-19 · commit `cc5e405`）

> 死规则清理 + 空亡断语修活。规则库 801→789（删 12 修 2），`matchRules` 神煞分支新增空亡特判（ENGINE 标记区内）。

| 项 | 值 |
|---|---|
| dist MD5 | `FEC924A713D83E38B3BDF81E4A055AA1` |
| dist 大小 | 247KB → **245KB**（RULES 内联规则 801 → 789） |
| 影响 C 端 | **是**（主面板规则命中量略降——单盘断语条数减少约 2~8 条；空亡规则由死变活（日支逢空亡盘新增 1 条断语）；建议 C 端回归报告篇幅与面板区间） |

**变更内容**：
1. **删除 12 条死规则**（0 命中）：7 条 SHENSHA 表外神煞（元辰/魁罡/太极贵人/福星贵人/国印贵人/天喜/咸池——咸池与桃花同盘矛盾、其余引擎未实现算法）+ `study_学业_魁罡路` + 4 条「状态:旺」六亲（`evalState` 无'旺'分支，且 2 对语义重复）。
2. **修复 2 条**：`shensha_文昌贵人`/`study_文昌配印` 神煞名「文昌贵人」→ 表内「文昌」（表内名是"文昌"，表外名永不命中）。
3. **修活 `shensha_空亡`**：`matchRules` 神煞分支特判 `ctx.kongWang` 且**限定日支逢空亡**（第一版「有空亡即命中」100% 命中被 audit 拦截，收紧后约 16%）。
4. 断言同步：test_engine RULES 801→789、verify_ux_e2e G0 六亲 48→44。13 套回归全绿 + audit 无 100% 命中 + check_conflicts 1000 盘 0 矛盾。

**bazi-app 注意事项**：C 端需 `cp engine/engine.dist.js web/`（已拷贝交付）后回归报告生成；MD5 核对 `FEC924A713D83E38B3BDF81E4A055AA1`。

---

## v1.2.1（2026-08-17 · commit `ed973d9` 推送点）

> ✅ **发布状态（2026-08-17 确认）**：SkillHub 已上架（申诉通过，「生态杀手」分类，平台显示 V1.0.0）+ ClawHub 已上架（Productivity 分类，自动同步本版本）

| 项 | 值 |
|---|---|
| dist MD5 | `d6409710ed764437f4ee03ac7c9e9019` |
| dist 大小 | 189KB → **247KB**（RULES 数据表内联，断语 504 → 801） |
| 影响 C 端 | **是**（规则库 +297 条 → C 端报告命中量增加约 59%，报告篇幅变化，需回归） |

**变更内容**：
1. **断语库 504 → 801 条**（+297）：事业+44 / 性格+37 / 财运+29 / 用神喜忌+26 / 婚姻+22 / 十神组合+22 / 学业+22 / 神煞+22 / 健康+21 / 格局+18 / 六亲+18 / 五行生克+16。主引擎 `matchRules` 条件匹配，全部含 suggestion + 古籍出处。命中分布审计无 100%/>80% 死规则。
2. **输入侧晚子时/节气临界提示语**（主表单 + 合婚 A/B）：UI 层实现，**dist 无变化**，但属引擎版本 v1.2.1 一部分。
3. 修复 7 条死规则（SHENSHA 表外神煞引用、evalState 缺位置/十神键）。

**bazi-app 注意事项**：C 端报告使用 `matchRules` 输出规则评语，规则量增大后单盘断语条数上升；若有单盘断语数上限或排版依赖，需回归确认。

---

## 历史版本（v1.2.0 及更早）

> 历史变更记录在 `HANDOFF.md`（§五 已完成内容 / §八 踩过的坑）与 `docs/history/`，本文件自 v1.2.1 起作为权威记录。

# bazi-master 第七轮 · 端到端用户视角验收报告

> 验收日期：2026-08-12 ｜ 验收人：小二（模拟 5 类真实用户画像）
> 范围：SKILL.md 引导入口 → 引擎排盘 → 报告渲染 → 输出稳定性，全链路用户视角

---

## 一、验收结论（先看这个）

**引擎正确性：全部通过。** 24 项断言 0 失败，4 套既有回归复跑 0 回归，150 盘命中分布无异常。

**但用户视角发现 8 个体验问题**，其中 2 个 P1（会导致**错误结果**或**能力承诺落空**）需要优先修复，4 个 P2（体验模糊/静默处理），2 个 P3（表述口径，已顺手修正）。

> 一句话总结：**"算得准"已经达标，"用起来稳"还有 2 个坑**——性别忘选会静默排错大运方向；时辰未知时承诺的"六字分析"引擎根本没有实现。

---

## 二、验收方法

| 用户画像 | 模拟行为 | 验证点 |
|---|---|---|
| 小白用户 A | 完整信息（姓名/日期/时间/性别/城市） | 正常出盘、报告完整 |
| 马虎用户 B | 无效日期、超范围年份、非法时刻、性别漏选 | 是否被友好拦截 |
| 迷糊用户 C | **时辰不记得**（SKILL.md 承诺降级六字分析） | 降级链路是否真实存在 |
| 边界用户 D | 晚子时 23:30 / 立春当日 / 节气交界 | 精确切分 + 是否向用户说明 |
| 复算用户 E | 同一命盘连算 20 次 | 输出一致性（防随机/状态污染） |

自动化脚本：`tools/verify_ux_e2e.js`（新增，已登记 SKILL.md 并同步打包目录）

---

## 三、自动化验收结果（24 项断言全 PASS）

### A. 输入容错（6/6 PASS）
| 用例 | 结果 |
|---|---|
| 正常输入出盘 | ✅ `庚午 辛巳 庚辰 癸未` |
| 无效日期 2/30 被拦截 | ✅ "日期无效（如 2 月没有 30/31 日）" |
| 超范围年份 1800 被拦截 | ✅ "超出支持范围（1895-2100）" |
| 小时 24 / 性别空被拦截 | ✅ 各有明确提示 |
| 时辰缺失（NaN） | ⚠️ 被拦截，但**无降级路径**（见问题 P1-2） |

### B. 边界场景（5/5 PASS）
| 用例 | 结果 |
|---|---|
| 晚子时 23:30 时柱 | ✅ 戊子（庚日+1=辛日，丙辛从戊起），四柱与 lunar-python 交叉验证一致 |
| 立春精确切分（15:00 vs 17:00） | ✅ 癸卯年 → 甲辰年 |
| 出生地未填 | ✅ 正常出盘，solarInfo=null |
| 未知城市 | ✅ 兜底提示"已用北京时间排盘" |
| 大雪后月柱 | ✅ 丙子（子月） |

### C. 报告质量（150 盘抽样）
| 指标 | 结果 |
|---|---|
| suggestion 建议覆盖率 | ✅ 100%（429/429） |
| 出处标注覆盖率 | ✅ 100% |
| 主面板 6 类平均命中 | ✅ 23.1 条/盘（性格2.0/事业6.3/财运4.2/婚姻3.0/健康5.1/格局2.6） |
| 单类 0 命中率 | ✅ 仅"十神组合"7%（其余全部 0%） |
| 单盘全分类 0 命中（引擎短路） | ✅ 150 盘无一出现 |

### D. 稳定性
| 用例 | 结果 |
|---|---|
| 同盘连算 20 次四柱+大运+神煞 | ✅ 输出完全一致（无随机性/状态污染） |

### E. 报告完整性
| 项 | 结果 |
|---|---|
| 大运 8 步 | ✅ |
| 神煞速查表 24 项（按命中展示） | ✅ 单盘命中 11 项 |
| 五行补救（3 项） | ✅ |
| 喜用/忌神 | ✅ 火木水 / 金土 |
| 空亡+胎元+命宫+身宫 | ✅ 申酉 / 壬申 / 己巳 / 乙亥 |

---

## 四、用户视角问题清单（8 项）

### 🔴 P1（会导致错误结果或能力落空，建议本轮修复）

**P1-1 性别选择有默认值"男"——忘选即排错大运方向**
- 现象：UI 表单 `<select id="gender">` 第一项即"男"，无"请选择"占位
- 影响：性别直接决定大运**顺排/逆排**，用户忘选会静默拿到方向完全相反的大运，且**无任何警示**。这是所有问题中唯一能"算错还不吭声"的
- 建议：加 `<option value="">请选择性别</option>` 置顶 + `generate()` 校验空值并 alert

**P1-2 "时辰未知→只做六字分析"的承诺，引擎没有实现**
- 现象：`paipan()` 要求 hh/mm 为有效数字（`Number.isFinite` 校验），UI 表单 time 必填（`if(!date||!time)`），全程无"时辰未知"选项
- 影响：SKILL.md 第一阶段与边界表都承诺"时辰未知则只做六字分析"，但唯一可行路径是 AI 手工推算——这又直接违反核心原则 1"排盘必须用代码/查表计算，绝不用 LLM 心算"。**两条规则互相矛盾，用户时辰未知时 skill 会卡死**
- 建议（三选一）：
  - A（推荐，改动小）：SKILL.md 明确降级路径——时辰未知时不打开 index.html，改由 AI 用 knowledge-base 基础表手工排年月日三柱 + 标注"时辰未知，仅作六字参考"
  - B（改动中）：UI 增加"时辰未知"复选框，引擎支持 6 字模式（时柱显示"未知"，跳过依赖时柱的规则/大运改为按 3 柱提示）
  - C（改动大）：完整实现 6 字引擎 + 大运降级，不建议现阶段做

### 🟡 P2（体验模糊/静默处理，建议下轮或随 P1 一并处理）

**P2-1 日期/时间有默认值**（1990-05-15 / 10:00）——用户忘填会静默用默认值出盘。建议默认留空（`generate()` 已有空值拦截）

**P2-2 晚子时无提示（非校正路径）**——SKILL.md 承诺"夜子时按次日日柱，**注明早晚子时法**"，但提示只存在于真太阳时校正分支（`build_ui.py:1176`）；用户选"否（用北京时间）"且 23 点后出生时，引擎静默用晚子时法却不说。建议在四柱表区域统一加提示

**P2-3 节气临界无提示**——SKILL.md 承诺"节气交界日提示可能两种月柱，分别简析"，但引擎只给唯一答案且不告知"距立春/换月仅差 X 分钟"。建议引擎输出临近节气边界的警告字段

**P2-4 SKILL.md 引导流程易误解**——"先问阳历生日→再问农历生日"会让用户以为两个都要给（实际只需其一）；"出生时辰选项：具体时间/时辰/不确定"中"时辰"是术语。建议改为"您的出生日期是公历还是农历？"单步确认 + 选项通俗化（"知道具体时间，如下午 2:30 / 只记得大概，如早上 / 完全不知道"）

### 🟢 P3（表述口径，**本轮已顺手修正**）

- **P3-1** SKILL.md"神煞（24 项吉凶星曜）"→ 已改为"24 项核心神煞速查表，按宫位分组、仅展示命局命中项"（24 是速查表规模，单盘只展示命中项属正确设计）
- **P3-2** SKILL.md"主面板 6 类平均 25-35 条/盘"→ 已改为"23-30 条（实测 150 盘 23.1）"，口径与实际一致

---

## 五、P1/P2 修复执行记录（2026-08-12 已执行）

| 编号 | 问题 | 修复内容 | 状态 |
|---|---|---|---|
| 🔴 P1-1 | 性别默认"男"，忘选则大运顺逆排反 | 主盘 + 合婚盘性别 select 均加"请选择"占位（value=""）；`generate()` / `readHe()` 增加性别必填校验（`请选择性别（用于排大运顺逆）`），未选则拦截不排盘 | ✅ 已修复 |
| 🔴 P1-2 | 承诺"时辰未知→六字分析"，引擎无入口 | SKILL.md 对齐：明确"无法精确排盘，禁止心算六字"；降级路径=引导回忆/估时辰→按时段代表时辰（凌晨寅/上午巳/下午申/晚上戌）排盘并在报告标注"时辰为估算"，完全未知时可选 1-2 个代表性时辰分别排盘做参考 | ✅ 已修复 |
| 🟡 P2-1 | 日期/时间有默认值，忘填静默出盘 | 主盘 + 合婚盘 date/time 默认值全部清空（4 处），未填时 `请填写出生日期与时间` 拦截 | ✅ 已修复 |
| 🟡 P2-4 | 引导"先阳历再农历"易误解、"时辰"术语晦涩 | 改为"生日（阳历或农历**给一个即可**，问清是哪种；农历确认是否闰月）"；"出生时辰"→"出生时间（24小时制几点几分，越精确越好，选项：知道具体时间/只知道大概时段/完全不知道）" | ✅ 已修复 |
| 🟡 P2-2 | 晚子时提示仅真太阳时路径 | 边界表已对齐引擎真实行为（真太阳时路径标注"已入晚子时"，非校正路径不标注）——文档层先行对齐，引擎增强留待评估 | ✅ 文档已对齐 |
| 🟡 P2-3 | 节气临界无提示 | 边界表改为"引擎以节气精确切分给出唯一月柱；距交节不足约 1 小时提示复核"——文档层先行对齐 | ✅ 文档已对齐 |

**回归验证**：`verify_ux_e2e.js` 新增 A7（性别未选拦截）/ A8（日期时间未填拦截）断言，5 套回归（test_ui / test_eval_state / test_p1_fixes / verify_sleep_rules / verify_ux_e2e）全 PASS，打包目录同步并独立运行通过。index.html 已重建（223KB，429 条断语，0 默认值残留，3 处"请选择"占位）。

## 六、下一步建议

1. **UI 视觉打磨**（index.html 用户直接可见，按高标准再磨一遍——拒绝 AI 味）
2. **市场发布物料**：Skill 描述、示例对话、封面图
3. **以个人身份发布**（与魅可完全解耦）
4. 可选增强（引擎层）：晚子时非校正路径提示、节气临界自动警告——属于引擎输出增强，发布后可做

---

*附：验收脚本已同步至打包目录（`~/.workbuddy/skills/bazi-master/tools/verify_ux_e2e.js`，13 个工具文件），SKILL.md 工具清单已登记，两处版本一致。*
